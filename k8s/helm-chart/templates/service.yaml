apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-gemini-validator-service
spec:
  selector:
    app: {{ .Release.Name }}-gemini-validator
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: 8080
  type: {{ .Values.service.type }}
